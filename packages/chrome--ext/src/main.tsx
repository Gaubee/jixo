// This file is the entry point for the Vite build of the popup/sidepanel HTML.
// In our new architecture, React components are dynamically rendered into the
// content script's dialog from `web/main.ts`.
//
// This file can be kept for potential future use cases where a static
// React app is needed, but for now, it does not need to do anything.

console.log("JIXO UI: main.tsx loaded, but rendering is controlled by content-script.");
